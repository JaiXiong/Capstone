package asset;

import java.awt.*;

public class ColorDefinitions {
    /**
     * For Jai and the world team.
     * To define custom colors, simply call new Color(r, g, b), like so:
     */
    // <-- IntelliJ helpfully shows this color in a square to the right of the line number.
    public static final Color CUSTOM_COLOR_EXAMPLE = new Color(225, 46, 137);
}
